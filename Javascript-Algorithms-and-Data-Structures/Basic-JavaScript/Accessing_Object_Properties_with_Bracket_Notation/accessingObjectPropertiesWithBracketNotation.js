// Setup:

var testObj =
{
  'an entree': 'hamburger',

  'my side': 'veggies',

  'the drink': 'water'
};

// Solution:

var entreeValue = testObj['an entree']; // equals 'hamburger'
var drinkValue = testObj['the drink'];  // equals 'water'
